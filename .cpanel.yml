---
deployment:
  tasks:
    - export DEPLOYPATH=/home/j2ykz030ddm5/public_html/uracarealtyph.com/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -a --exclude='.git' --exclude='.cpanel.yml' --exclude='.gitignore' --exclude='*.md' --exclude='config/config.local.php' --exclude='storage/sessions/*' ./ $DEPLOYPATH
    - /usr/bin/find $DEPLOYPATH -type d -exec /bin/chmod 755 {} \;
    - /usr/bin/find $DEPLOYPATH -type f -exec /bin/chmod 644 {} \;
